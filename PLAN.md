If you want next, I can add:
 1. cascade revoke (revoke TOTP -> optionally revoke 
all
 linked clients), 2. a first-class phone invite flow 
 (single one-time onboarding payload + QR), and 3. 
 explicit geo provider toggle in Settings 
 (off/local/remote).
                                                           
 do all. the rule is only one client per TOTP. new 
clients . make it impossible in the api.
