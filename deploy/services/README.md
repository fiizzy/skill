# Service Templates

These are starter templates for production daemon management.

- `macos/com.skill.daemon.plist` — LaunchAgent template
- `linux/skill-daemon.service` — systemd --user template
- `windows/install-service.ps1` — Windows Service install script

> Paths are placeholders and must be adjusted during installer integration (DMG/NSIS).
